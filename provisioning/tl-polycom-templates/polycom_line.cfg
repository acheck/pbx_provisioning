       	reg.${LINE}.displayName="${DISPLAY_NAME}"
        reg.${LINE}.server.1.address="${SERVER}"
        reg.${LINE}.server.1.transport="UDPonly"
        reg.${LINE}.server.2.transport="UDPonly"
        reg.${LINE}.address="${USERID}"
        reg.${LINE}.label="${EXTENSION}"
        reg.${LINE}.auth.userId="${USERID}"
        reg.${LINE}.auth.password="${PASSWORD}"
	reg.${LINE}.outboundProxy.address=""
	reg.${LINE}.outboundProxy.port="5060"
	reg.${LINE}.outboundProxy.transport=""
	reg.${LINE}.lineKeys="${LINE_KEYS}"
	reg.${LINE}.callsPerLineKey="8"
        reg.${LINE}.nat.traversal.mode="Auto"
        voIpProt.SIP.requestValidation.${LINE}.method="source"
        voIpProt.SIP.requestValidation.${LINE}.request="INVITE"
        feature.nat.stun.enabled="0"
	msg.mwi.${LINE}.callBackMode="contact"
	msg.mwi.${LINE}.callBack="*98"
	msg.mwi.${LINE}.subscribe="${USERID}"